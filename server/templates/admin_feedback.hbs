<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .container {
            width: 60%;
            margin: 40px auto;
            background: #ffffff;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        .container:hover {
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.15);
        }
        .header {
            background: linear-gradient(135deg, #007BFF, #00C6FF);
            color: white;
            text-align: center;
            padding: 15px;
            border-radius: 8px 8px 0 0;
            font-size: 22px;
            font-weight: bold;
        }
        .details {
            background: #f9f9f9;
            padding: 20px;
            border-radius: 8px;
            margin-top: 15px;
            box-shadow: inset 0px 2px 5px rgba(0, 0, 0, 0.05);
        }
        p {
            font-size: 16px;
            color: #555;
            line-height: 1.8;
        }
        .highlight {
            font-weight: bold;
            color: #007BFF;
        }
        .footer {
            margin-top: 25px;
            text-align: center;
            font-size: 14px;
            color: #777;
            padding-top: 10px;
            border-top: 1px solid #ddd;
        }
        .footer p {
            font-size: 13px;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="header">
        📩 New Feedback Received
    </div>

    <p><strong>Name:</strong> <span class="highlight">{{name}}</span></p>
    <p><strong>Email:</strong> <span class="highlight">{{email}}</span></p>
    <p><strong>Contact:</strong> <span class="highlight">{{contact}}</span></p>

    <div class="details">
        <p><strong>Message:</strong></p>
        <p>{{message}}</p>
    </div>

    <div class="footer">
        <p>⚡ This is an automated email. Please do not reply. ⚡</p>
    </div>
</div>

</body>
</html>
